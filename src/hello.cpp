// Day 1 "hello CMake": proves the C++ toolchain and build path work.
// Kept as a trivial smoke-test target; the real work is in parse_trades.
#include <iostream>

int main() {
    std::cout << "gpu-inference-trading: build OK (C++ toolchain working)\n";
    return 0;
}
